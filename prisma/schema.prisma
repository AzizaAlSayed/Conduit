generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id          String @id @default(uuid())
  userName    String @unique
  title       String @unique
  bio         String
  description String
  tags        String
}
